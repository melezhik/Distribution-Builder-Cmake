image:
  - melezhik/sparrow:alpine
tasks:
  -
    name: test
    language: Bash
    code: |
      set -e
      cd source/
      zef install . --deps-only --test-depends --build-depends --/test -to=home         
      zef test --verbose .
          
